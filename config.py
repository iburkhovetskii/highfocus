"""
Конфигурация для бота High Focus
"""
import os
from dotenv import load_dotenv

load_dotenv()


class Config:
    """Настройки бота"""
    
    # Токен бота
    BOT_TOKEN = os.getenv("BOT_TOKEN")
    
    # База данных
    DB_PATH = os.getenv("DB_PATH", "highfocus.db")
    
    # Настройки логирования
    LOG_LEVEL = os.getenv("LOG_LEVEL", "INFO")
    
    # Канал для подписки
    CHANNEL_URL = os.getenv("CHANNEL_URL", "https://t.me/high_focusEN")
    CHANNEL_ID = os.getenv("CHANNEL_ID", None)  # Опционально для проверки подписки
    
    # Тексты сообщений (можно выносить в отдельный файл для легкого изменения)
    MESSAGES = {
        "start": """⚡️ Привет! Это High Focus — напиток для тех, кто держит ум в тонусе.

Мы созданы, чтобы помогать сохранять концентрацию, энергию и баланс в мире, где информации больше, чем времени.

Хочешь узнать, какой у тебя тип фокуса — и какой вкус High Focus включает тебя на максимум? ⚡️""",
        
        "about": """High Focus — это инновационный молочный напиток от ЭкоНивы, созданный для тех, кто работает головой.

В составе:
☕️ гуарана — мягкая энергия,
🍃 L-теанин — концентрация и баланс,
💊 витамины группы B — поддержка мозга и настроения.

Без сахарозы. Без лактозы. Только чистый фокус и энергия.""",
        
        "divider": "💬 И последние 2 вопроса — чтобы мы лучше понимали, что тебе интересно 👇",
        
        "final": """Хочешь попробовать свой вкус прямо сейчас?
Подпишись на наш канал, покажи нам на стенде и получи подарок 🎁
А если уже дегустировал — приходи сыграть в наши активности и собери стикеры ⚡️""",
        
        "subscribed": """Отлично! 🎉

Приходи на наш стенд, покажи подписку и получи подарок 🎁
Увидимся! ⚡️"""
    }
    
    @classmethod
    def validate(cls):
        """Проверка наличия обязательных параметров"""
        if not cls.BOT_TOKEN:
            raise ValueError("BOT_TOKEN не найден в переменных окружения!")
        return True


# Валидация при импорте
if __name__ != "__main__":
    Config.validate()

